using UnityEngine;

namespace DvMod.ZSounds.UI
{
    /// <summary>
    /// MonoBehaviour component that renders separate windows outside of ModToolbarAPI context
    /// (e.g., editor windows that need to be standalone GUI.Window instances)
    /// </summary>
    public class SoundManagerButton : MonoBehaviour
    {
        void OnGUI()
        {
        }
    }
}
